# KURUI 開発奮闘記 & 技術メモ (Development Log)

TRIMUI Model S (Powkiddy A66) 向け超軽量ランチャー「**KURUI**」を、純粋な C言語 と SDL 1.2 のみでゼロから開発・実機動作させるまでに直面した数々のハードル、仕様の罠、そして解決策の全記録です。

---

## 1. ターゲット環境とスペックの壁

* **デバイス**: TRIMUI Model S (Powkiddy A66)
* **SoC**: Allwinner F1C100s (ARM926EJ-S, ARMv5TE, 最大 533MHz)
* **メモリ**: **32MB (内蔵SIP)**
* **ディスプレイ**: 2.0インチ IPS LCD (320x240, 60Hz, 16-bit RGB565)
* **OS**: 軽量組み込みLinux (カーネル 5.x / 4.x, uClibc / GLIBC 2.4+ 混在)
* **ストレージ**: MicroSD カード (`/mnt/SDCARD`)

メモリがわずか32MBしかなく、スワップも存在しないため、モダンなリッチフレームワークや重いランタイムは一切使用できません。常時60FPSを維持しつつ、ゲーム起動時にメモリとフレームバッファを完全に解放できる「極限の軽量さ」が求められました。

---

## 2. 外部ライブラリゼロ（SDL 1.2 Pure C）の設計

### 2.1 SDL_ttf / SDL_mixer の排除
実機のファームウェア環境にどのライブラリが存在するか（あるいはバージョン不整合が起きるか）に左右されないよう、リンク対象を標準の `libSDL-1.2.so.0` と `libc.so.6` の2本のみに限定しました。

* **フォント表示**:
  * 8x8 ドットのビットマップフォントテーブル（美咲フォント準拠）をヘッダ内に内蔵。
  * `SDL_Surface` のピッチと `BytesPerPixel` を直接参照し、RGB565 (16bit) / ARGB (32bit) を判定して直接ピクセルを叩き込むダイレクトレンダリングエンジンを自作。
  * UTF-8 デコーダを実装し、日本語のマルチバイト文字をUnicodeコードポイントへ変換、テーブルをバイナリサーチ（2分探索）で超高速検索。
* **サウンド再生**:
  * SDL の低レイテンシ・生オーディオコールバック（`AUDIO_U8`, 22050Hz, 256 samples）を自前で実装。
  * 音声ファイル（WAV/MP3）を持たず、PSG（プログラマブル・サウンド・ジェネレータ）方式で位相アキュムレータを回し、ゲームボーイ起動音（C6: 1046.5Hz → C7: 2093Hz のピッチスイープ＋線形減衰エンベロープ）の矩形波を完全数式合成。
  * 外部音声アセットが不要で、遅延0msの「ピコーン！」を実現。

---

## 3. 直面したトラップと解決策（死闘の記録）

### トラップ①: 実機 GLIBC バージョン不整合（GLIBC_2.34 の壁）
* **現象**: ビルドしたバイナリを実機で動かそうとすると、`libc.so.6: version 'GLIBC_2.34' not found` で即死する。
* **原因**:
  * GitHub Actions の標準ランナー (`ubuntu-latest` / Ubuntu 22.04 以降) に搭載されているクロスコンパイラでビルドしたため。
  * 近年の glibc (2.34以降) は `libpthread.so` などの関数が `libc.so.6` に統合されており、生成されたバイナリが新しい GLIBC シンボルを要求してしまう。
  * 実機の `ld-linux.so.3` は極めて古く、要求バージョンは **`GLIBC_2.4`** だった。
* **解決策**:
  * CIのコンパイル環境を `ubuntu:20.04` (Focal, GLIBC 2.31) コンテナに固定。
  * リンクオプションから `-lpthread` を完全に排除し、`EXTRA_LIBS="-L/tmp/sdl_arm -lSDL -Wl,--as-needed"` で最小限のリンクに制限。
  * これにより、出力バイナリの要求バージョンを実機が許容する `GLIBC_2.4` まで引き下げることに成功。

### トラップ②: Ubuntu/Debian コンテナのマルチアーキ APT 崩壊
* **現象**: `dpkg --add-architecture armel` して `libsdl1.2-dev:armel` をインストールしようとすると、Ubuntu/Debian の ports サーバ分離や Bullseye のアーカイブ落ちにより apt が 404 / 壊れた依存関係でクラッシュ。
* **解決策**:
  * 外部の armel 版 SDL パッケージをダウンロードするのを諦め、標準の `gcc-arm-linux-gnueabi` ツールチェーンのみを使用。
  * ビルドスクリプト内で、ダミー関数のみを含む `stub_sdl.c` を `arm-linux-gnueabi-gcc -shared -Wl,-soname,libSDL-1.2.so.0` でコンパイルし、ビルド時専用のリンク用スタブライブラリを動的生成。
  * 実機上では実機内蔵の `/usr/lib/libSDL-1.2.so.0` が実行時にリンクされるため、CI側で巨大なマルチアーキ開発パッケージを導入する必要が完全に消滅。

### トラップ③: TRIMUI の ZIP 認識ルール（FW 0.106 vs 0.105）
* **現象**: 配布用 ZIP を SDカードに入れても、本体の「FILE」メニューにファイルが表示されない。
* **原因**:
  * TRIMUI Model S のファームウェア仕様:
    * **FW 0.105 以前**: `TrimuiUpdate_*.zip` という前方一致しか認識しない。
    * **FW 0.106 以降**: `trimui_*.zip` という小文字前方一致しか認識しない。
* **解決策**:
  * CIリリースで `trimui_kurui.zip` と `TrimuiUpdate_kurui.zip` の両方を自動生成して同時配布。

### トラップ④: 恐怖の「画面に 00% が出てフリーズ」事件
* **現象**: FILE メニューから ZIP を実行したところ、画面に「00%」が表示されたまま一切操作を受け付けず固まる。
* **原因**:
  * TRIMUI のメインUIは、ZIPが選択されると更新用バイナリ `updateui` を起動する。
  * この `updateui` は Linux の仮想端末（VT）をグラフィックモードに切り替え、`VT_LOCKSWITCH` で**フレームバッファ (`/dev/fb0`) とコンソールを排他ロック**して進捗「00%」を描画する。
  * 当初「即時起動したい」と考え、スクリプト内で `killall updateui` を実行したところ、`updateui` が正常な終了処理（`FB_LeaveGraphicsMode` / `VT_UNLOCKSWITCH`）を行わずに死に、画面の排他ロックが残ったまま 00% の残像で固まってしまった。
* **解決策**:
  * TRIMUI 公式のプロセス間通信コマンド `notify` を使用する正規フローに修正：
    1. `notify 10 update "Preparing..."` で進捗を進める。
    2. ファイルの配置完了後、`notify 100 quit` を送信。
    3. これを受け取った `updateui` が自身で `FB_LeaveGraphicsMode` と `VT_UNLOCKSWITCH` を呼んでクリーンに終了。
    4. 直後に `reboot` を実行し、起動フック（`trimui_init.sh`）からまっさらな状態で KURUI を自動起動させる。

### トラップ⑤: MicroSD カードのディレクトリ名大文字小文字問題
* **現象**: Windows (FAT32) 上では大文字小文字が区別されないが、実機の Linux 上では `/mnt/SDCARD/Apps` と `/mnt/SDCARD/apps` が別物として扱われる。
* **原因**: GMenuNX や公式アプリランチャーのパスが `Apps` (先頭大文字) であるのに対し、古いスクリプトが小文字 `apps` を参照してファイルが見つからなかった。
* **解決策**: すべてのスクリプトと配置先を `/mnt/SDCARD/Apps/kurui` に統一。

### トラップ⑥: 豆腐（四角枠 `[•]`）だらけの文字化け
* **現象**: 実機起動後、英語は完璧なのに「スーパーマリオ」が「スーローアリオ」、「ゼルダの伝説」が「ロVよ0ロロ」のような謎の古代文字になる。
* **原因**:
  * 8x8 ドットフォントテーブルに濁音・半濁音（パ、ゼ、ポ、グ など）や一部の漢字（伝、説、面、終、了 など）が未定義だった。
  * 未定義文字に対してフォントエンジンがフォールバック用グリフ `[•]`（四角の中にドットがある豆腐パターン）を描画していたため、文字が潰れて見えていた。
* **解決策**:
  * ひらがな・カタカナの全音・濁音・半濁音、およびメニュー・タイトルで使われる全漢字を網羅して登録。
  * 「テ」などの紛らわしいドットバランスを美咲フォント基準でリファイン。

### トラップ⑦: 爆速でズササッと落ちてくるロゴ
* **現象**: ゲームボーイ風の起動アニメーションを実装したが、落下速度が毎フレーム +3px に設定されていたため、実機で動かした際に「上からものすごいスピードで落ちてきて激突するシュールな光景」になっていた。
* **解決策**:
  * 毎フレーム +1px に変更し、約1.8秒かけてスーッと滑らかに中央へ降りてきて、ピタッと静止すると同時に「ピコーン！」と鳴る本家ライクな優雅なテンポに調整。

---

## 4. アーキテクチャ構成

```
kur-ui/
├── .github/workflows/build.yml   # Ubuntu 20.04 コンテナによる GLIBC 2.4 互換ビルド & リリース自動化
├── Makefile                      # ARM クロスコンパイル / ホストPCテスト両対応
├── DEVELOPMENT_LOG.md            # 本開発ドキュメント
├── README.md                     # プロジェクト概要 & インストール手順
├── scripts/
│   ├── trimui_init.sh            # SDカード直下に置く起動フック（実機起動時に自動実行）
│   ├── updater                   # TRIMUI FILEメニュー用更新スクリプト
│   └── update.sh                 # updater と同内容の互換スクリプト
└── src/
    ├── main.c                    # メインループ (60FPS固定、安全なゲーム起動・終了復帰)
    ├── boot.h / boot.c           # GB風滑らかロゴ降下アニメーション
    ├── sound.h / sound.c         # SDLオーディオコールバックによるPSG矩形波合成 (遅延0)
    ├── font.h / font.c           # 16/32bit対応ダイレクトピクセル描画 & UTF-8 / バイナリサーチ
    └── font_data.h               # ASCII + 日本語（ひらがな・カタカナ・漢字）8x8 ビットマップデータ
```

---

## 5. 今後の拡張アイデア

* **ROM自動スキャン機能**: `/mnt/SDCARD/Roms/` 以下の各エミュレータフォルダを走査して動的リスト生成。
* **Teenage Engineering UIのさらなる洗練**: ボリューム・輝度インジケータ、バッテリー電圧連動のパーセント表示。
* **カスタムカラーテーマ**: オフホワイト、マットブラック、クラシックゲームボーイカラーの切り替え。
